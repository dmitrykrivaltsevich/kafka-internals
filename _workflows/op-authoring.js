export const meta = {
  name: 'kafka-ops-manual-authoring',
  description: 'Author Part II (Operations Manual): 13 source-grounded operational chapters in the bespoke-diagram HTML system, each adversarially fact-checked',
  phases: [
    { title: 'Author', detail: 'one agent per chapter: source + research grounded, bespoke diagrams, legends' },
    { title: 'Verify', detail: 'adversarial fact-check against source + sanity of empirical claims' }
  ]
};

const ROOT = '/Users/user/projects/_playground/apache-kafka-architecture/kafka-source';
const OUT  = '/Users/user/projects/_playground/apache-kafka-architecture';
const RESEARCH = OUT + '/_research/ops-blueprint-reference.md';

const XLINKS =
'Part I (Architecture Internals) chapters to cross-link by filename: 00-overview, 01-record-format, 02-wire-protocol, 03-storage-log-engine, 04-storage-management, 05-tiered-storage, 06-network-and-threading, 07-request-processing, 08-replication, 09-fetch-path, 10-kraft-consensus, 11-kraft-controller, 12-metadata-propagation, 13-group-coordination, 14-transactions-eos, 15-share-groups, 16-producer-client, 17-consumer-client, 18-security, 19-quotas, 20-kafka-streams, 21-kafka-connect, glossary.\n' +
'Part II (this part) chapters: op00-operator-model, op01-configuration, op02-limits, op03-partitioning, op04-capacity-planning, op05-performance-tuning, op06-durability, op07-failure-modes, op08-metrics-signals, op09-topologies, op10-cost, op11-scaling-scenarios, op12-lifecycle.';

const HOUSE =
'You are a principal Kafka SRE and architect writing Part II — THE OPERATIONS MANUAL — of a high-end Apache Kafka book-site, for Apache Kafka 4.4.0-SNAPSHOT (git 04bfe7d, KRaft-only; ZooKeeper removed in 4.0). This is the practical operator guide: limits, tunables, capacity & partition sizing, failure runbooks, the signals to watch, cost, topologies, and what changes at scale.\n\n' +
'TWO GROUNDING SOURCES:\n' +
'1. THE SOURCE CODE at ' + ROOT + ' — ground truth for configs, DEFAULT VALUES, hardcoded constants/limits, mechanisms, metric names. Read the real files. Cite as path/File.ext:line. EVERY configuration default, limit, and mechanism claim must come from the source you read.\n' +
'2. THE EMPIRICAL REFERENCE at ' + RESEARCH + ' — read it for the numbers the code cannot give: real benchmarks, capacity formulas, sizing heuristics, partition-count guidance, cost figures, scaling case studies (each fact there carries its external source). Use it for empirical claims and CITE the source named there (e.g. "Confluent benchmark", "Jun Rao, 2015", "LinkedIn"). Mark empirical/version-dependent numbers as such — do not present a benchmark number as if it were a guarantee.\n\n' +
'THE CARDINAL RULE — every recommendation, default, and limit must carry its WHY, rooted in a concrete source mechanism or architectural property (cross-link the Part I chapter that explains the mechanism). "Set min.insync.replicas=2 because ..." where the because is a real mechanism in the code, not folklore. Distinguish clearly: HARD limits (a compiled-in constant — give the constant and file), SOFT limits (a config — give the key + default), and EMERGENT limits (a consequence of resources/architecture — explain the bound). Where a tradeoff exists, say whether it is tunable and what the tuning range/dial is.\n\n' +
'ACCURACY OVER EVERYTHING. A precise, source-grounded operational fact beats a plausible generalization. Never invent a config key, default, or metric name — verify it in source. If you cannot verify, omit or mark "(unverified)".\n\n' +
'Be genuinely useful to an engineer who must run this in production: concrete numbers, formulas, decision rules, thresholds, and "do this / watch this" guidance. Aim for 3,000–6,000 words of dense, correct, practical content per chapter.';

const COMPONENTS =
'OUTPUT = an HTML FRAGMENT for inside <article> (NO <html>/<head>/<body>/<nav>/<script>; a build wraps it and auto-builds the sidebar + on-page TOC + prev/next from your headings). Begin with <h1> "NN · Title" (use the provided display number, e.g. "II · 03"), then <blockquote class="provenance">Source: Apache Kafka 4.4.0-SNAPSHOT (git 04bfe7d, 2026-06-15), KRaft mode. Operational guidance grounded in source code and cited benchmarks.</blockquote>, then <p class="lead">…abstract…</p>, then the body with <h2>/<h3> (no id attributes).\n\n' +
'TEXT: <p>, <ul>/<ol>/<li>, <strong>, <code> for identifiers/config-keys/metrics/paths. Source citations inline as <code>path/File.java:123</code>. Cross-links: <a href="op03-partitioning.html">…</a> and to Part I <a href="08-replication.html">…</a>. KIP badges <span class="pill kip">KIP-392</span>.\n' +
'TABLES (use heavily for config/limit/metric references): <div class="table-wrap"><table><thead>…</thead><tbody>…</tbody></table></div>.\n' +
'CALLOUTS: <div class="callout key|rationale|invariant|gotcha|warning|note"><span class="callout-title">…</span><p>…</p></div> — use "rationale" for the WHY, "gotcha"/"warning" for operational traps, "key" for rules of thumb.\n' +
'BESPOKE DIAGRAMS — NO Mermaid, NO ASCII art. Use these CSS components (every diagram lives in <figure class="diagram">…</figure> and MUST end with a <div class="legend">). Semantic colours: cat-client (blue), cat-broker (purple), cat-storage (green=log/disk/internal-topic), cat-control (amber=controller/metadata), cat-coord (teal=coordinator/wait), cat-error (red=failure).\n' +
'  • dflow — flows / decision trees / runbook paths (the workhorse for ops). Vertical, add class "h" for horizontal:\n' +
'    <figure class="diagram"><div class="dflow"><div class="node cat-broker"><span class="nt">symptom: URP &gt; 0</span></div><div class="conn" data-label="check"></div><div class="node cat-storage decision">ISR shrinking?</div><div class="conn"></div><div class="row"><div class="col"><div class="conn" data-label="yes"></div><div class="node">slow follower / network</div></div><div class="col"><div class="conn ec-err" data-label="no"></div><div class="node cat-error">broker down</div></div></div></div><figcaption>…</figcaption><div class="legend">…</div></figure>\n' +
'    node modifiers: "store" (cylinder), "decision" (rounded), "pill". connector .conn: data-label, "dashed" (async), "ec-data"/"ec-async"/"ec-err". Use .row + .col for branches (a connecting fork manifold is drawn automatically).\n' +
'  • dstack — layered architecture / planes / topology stacks: <div class="dstack"><div class="layer cat-control"><div class="ln">…</div><div class="ld">…</div><div class="row"><span class="chip">Class</span></div></div><div class="sep">↕</div><div class="layer cat-broker">…</div></div>\n' +
'  • dseq — protocol/operation sequences over time: <div class="dseq" style="--cols:3"><div class="lanes"><span class="actor cat-client">Admin</span>…</div><div class="body"><div class="lifelines"><i></i><i></i><i></i></div><div class="m r" style="--from:1;--to:2"><span class="ml">AlterPartitionReassignments</span><span class="ln"></span></div>…</div></div> (m class "r"/"l" = arrow right/left; "dashed" reply).\n' +
'  • dstate — state machines: <div class="dstate"><span class="term"></span><span class="tr"><span class="tl"></span><span class="ta"></span></span><span class="st">Online</span><span class="tr"><span class="tl">disk fails</span><span class="ta"></span></span><span class="st warn">Offline</span>…<span class="term end"></span></div>\n' +
'  • logstrip — partition offset / lag visuals: <div class="logstrip"><div class="ls-head"><span class="ttl">partition</span><span class="append">→</span></div><div class="cells"><div class="cell committed">…<span class="tag hw">HW</span></div><div class="cell next">…<span class="tag leo">LEO</span></div></div></div>\n' +
'  • bytemap — for fixed-width layouts (rarely needed in ops): <div class="bytemap"><div class="bf" style="--w:8"><span class="bf-name">…</span><span class="bf-meta">…</span></div></div>\n' +
'  • For capacity formulas / number tables, prefer a clean <table> or a <dl class="kv"><dt>…</dt><dd>…</dd></dl>.\n' +
'LEGEND content: explain only the encodings used (colour swatches <span class="sw cat-X"></span>, arrow samples <span class="arr"></span>/<span class="arr dashed"></span>, <span class="mono">…</span> for typography). \n' +
'SAFETY: never put a raw < > & inside <pre>/<code>/diagram text — escape as &lt; &gt; &amp; (use Unicode ≥ ≤ → in labels). Well-formed HTML, every tag closed.';

const SUBSYSTEMS = [
  { n: 'op00', disp: 'II · 00', slug: 'op00-operator-model', title: "The Operator's Mental Model",
    focus: 'What an operator actually runs and reasons about in KRaft 4.x. Nodes and process.roles (broker / controller / both via KafkaRaftServer); the metadata plane (controller quorum) vs the data plane (brokers) and what each owns. The control loops that keep a cluster healthy (broker heartbeats & fencing, ISR maintenance, high-watermark advancement, metadata propagation, consumer-group rebalancing) — each as a feedback loop the operator observes. The internal topics you operate and what breaks if they are unhealthy: __cluster_metadata, __consumer_offsets, __transaction_state, __remote_log_metadata, __share_group_state. The four SLIs that matter (availability, durability, end-to-end latency, consumer lag) and turning them into SLOs. A one-screen map of "what can go wrong, where" that the rest of Part II expands.',
    files: 'core/src/main/scala/kafka/server/{KafkaRaftServer.scala, BrokerServer.scala, ControllerServer.scala, SharedServer.scala}; server/src/main/java/org/apache/kafka/server/config/AbstractKafkaConfig.java; the internal topic names (grep for "__consumer_offsets","__cluster_metadata","__transaction_state","__remote_log_metadata"). Cross-ref Part I 06,08,11,12,13,14.' },

  { n: 'op01', disp: 'II · 01', slug: 'op01-configuration', title: 'Configuration: The Tuning Surface',
    focus: 'The configuration system as the operator\'s control surface. The hierarchy and precedence: static (server.properties, restart required) vs dynamic cluster-wide and per-broker (DynamicBrokerConfig, applied without restart, stored as metadata records) vs per-topic (LogConfig overrides) vs client configs; how AbstractConfig.define() defines key/default/importance/validator and how defaults are resolved. How to read a default from source. WHICH configs are dynamically alterable vs need a restart, and why (the dynamic-config update path). The ~20 broker/topic knobs that actually matter and their interactions: replication.factor, min.insync.replicas, unclean.leader.election.enable, log.retention.{ms,bytes}, log.segment.bytes, max.message.bytes, num.partitions, num.io.threads, num.network.threads, num.replica.fetchers, log.flush.* , compression.type, message.timestamp.*, log.cleanup.policy, quotas. For each: what it does, default, dynamic?, and the mechanism it tunes (cross-link Part I). Tunable vs hardcoded.',
    files: 'clients/src/main/java/org/apache/kafka/common/config/AbstractConfig.java; core/src/main/scala/kafka/server/DynamicBrokerConfig.scala; server/src/main/java/org/apache/kafka/server/config/{AbstractKafkaConfig.java, ServerConfigs.java, ServerLogConfigs.java, ReplicationConfigs.java}; storage/src/main/java/org/apache/kafka/storage/internals/log/LogConfig.java; clients/src/main/java/org/apache/kafka/common/config/TopicConfig.java. Cross-ref Part I 04,08,06,07,19.' },

  { n: 'op02', disp: 'II · 02', slug: 'op02-limits', title: 'Limits & Boundaries: Hard, Soft & Emergent',
    focus: 'The definitive limits catalogue, each classified HARD (compiled constant — name it + file:line), SOFT (config + default), or EMERGENT (resource/architecture bound — explain it). PARTITIONS: per-broker ceiling (Linux vm.max_map_count default 65530 with ~2 mmap per partition → ~32k; memory; open files; recovery/replay time) and per-cluster (KRaft metadata-log replay & controller — millions feasible vs the ZooKeeper-era ~4000/broker, ~200000/cluster; cite research). MESSAGE/BATCH: max.message.bytes (topic, default ~1 MiB), message.max.bytes/replica.fetch.max.bytes/fetch.max.bytes interplay (a too-large message vs replica fetch can wedge replication), producer max.request.size. REQUEST PLANE: queued.max.requests=500, max.incremental.fetch.session.cache.slots=1000, the per-request partition cap (MAX_REQUEST_PARTITION_SIZE_LIMIT=2000), connection caps (max.connections / per-ip, default Integer.MAX_VALUE). STORAGE: 2 GiB segment ceiling (a segment\'s byte position is a 32-bit int — log.segment.bytes max Integer.MAX_VALUE; the offset/time index entry sizes), offset is int64 (effectively unbounded). CONTROLLER batch limits (MAX_RECORDS_PER_BATCH=10000, MAX_PARTITIONS_PER_BATCH=10000). Explicitly answer the user\'s questions: "what is the max number of partitions per topic / per cluster?" and "is ~1 GiB/s per topic a hard limit?" — NO; topic throughput is emergent = (partitions) × (per-partition throughput ceiling) and is bounded by leader-broker NIC/disk and replication, not a compiled limit; show the reasoning.',
    files: 'server-common/src/main/java/org/apache/kafka/server/config/ServerConfigs.java (MAX_REQUEST_PARTITION_SIZE_LIMIT, fetch session); server/src/main/java/org/apache/kafka/network/SocketServerConfigs.java (MAX_REQUESTS=500, connections); server/src/main/java/org/apache/kafka/server/config/ServerLogConfigs.java (message bytes); storage/src/main/java/org/apache/kafka/storage/internals/log/{LogConfig.java, AbstractIndex.java, LogSegment.java}; metadata/src/main/java/org/apache/kafka/controller/{QuorumController.java, ReplicationControlManager.java} (MAX_*_PER_BATCH); the empirical reference for partition-limit numbers. Cross-ref Part I 03,08,11.' },

  { n: 'op03', disp: 'II · 03', slug: 'op03-partitioning', title: 'Partitioning Strategy: How Many Partitions?',
    focus: 'The central operational question, answered with mechanism + numbers. WHY partitions are the unit of (a) producer parallelism, (b) consumer parallelism (a group can have at most one consumer per partition — idle consumers beyond partition count), and (c) ordering (only per partition, per key via hash(key)%N). Per-partition throughput ceilings (~10 MB/s as a planning number — cite Jun Rao/research; varies with batch/compression/acks/RF). The SIZING HEURISTIC partitions = max(T/Tp, T/Tc) (Jun Rao 2015) — explain each term. The COST of partitions, each rooted in a mechanism: controller/metadata load and failover/leader-election time (~5 ms/partition historically — research), end-to-end latency rising past ~1000 partitions (research), open file descriptors and ~2 memory-mapped index regions per partition, producer client memory (a buffer per partition), replication fan-out (RF × fetcher work), and rebalance time. When to RESHARD 2→3→5: the trigger signals (sustained consumer lag with consumers == partitions, per-partition throughput saturated, needing more consumer parallelism). THE REPARTITIONING TRAP: you cannot decrease partitions, and increasing them on a keyed topic remaps hash(key)%N → breaks per-key ordering and co-partitioned joins/state → the accepted fix is a new topic at the target count + migration. Over-partitioning: provision for growth, but each partition has standing cost — give a concrete recommendation.',
    files: 'clients/src/main/java/org/apache/kafka/clients/producer/internals/BuiltInPartitioner.java; metadata/src/main/java/org/apache/kafka/controller/ReplicationControlManager.java (createPartitions); group-coordinator (assignment = partitions/consumers); the empirical reference heavily. Cross-ref Part I 03,08,11,16,17,13.' },

  { n: 'op04', disp: 'II · 04', slug: 'op04-capacity-planning', title: 'Capacity Planning & Sizing',
    focus: 'Turning a target workload into a cluster. The throughput model: ingress B/s = msg_rate × avg_size; REPLICATION amplification — each produced byte is fetched by (RF-1) followers, so inter-broker replication traffic = ingress × (RF-1); EGRESS = ingress × consumer_fanout + replication. DISK = ingress × retention_seconds × RF (local) — and how tiered storage changes the local term. MEMORY: the page-cache working set (Kafka serves caught-up consumers from page cache via zero-copy, so RAM should hold the "hot tail" of each partition; heap stays modest, ~6 GB typical — research) — do NOT oversize the JVM heap at the page cache\'s expense (mechanism: FileRecords/sendfile, Part I 03/09). NETWORK: NIC must carry ingress×RF (replication) + egress; this is often the first ceiling. CPU: compression, TLS, request handling. BROKER COUNT = max(target_throughput / per_broker_ceiling, total_partitions / per_broker_partition_limit, RF, 3). Worked example with real numbers. Headroom for failure (size for N-1 brokers) and burst.',
    files: 'storage/src/main/java/org/apache/kafka/common/record/internal/FileRecords.java (sendfile/zero-copy); core replication (Part I 08/09); the empirical reference formulas. Cross-ref Part I 03,08,09,05.' },

  { n: 'op05', disp: 'II · 05', slug: 'op05-performance-tuning', title: 'Performance Tuning: Throughput ⇄ Latency',
    focus: 'The tuning knobs as a throughput-vs-latency dial, each with its source mechanism. PRODUCER: batch.size + linger.ms (the batching dial — accumulate to amortize, at a latency cost; mechanism = RecordAccumulator, Part I 16), compression.type (none/lz4/zstd/snappy/gzip tradeoffs — research numbers; batch-level), acks (durability vs latency), max.in.flight.requests.per.connection (ordering with idempotence), buffer.memory + max.block.ms (backpressure). CONSUMER: fetch.min.bytes + fetch.max.wait.ms (read batching), max.partition.fetch.bytes, max.poll.records + max.poll.interval.ms (processing budget vs liveness). BROKER: num.io.threads (request handlers; CPU-bound; default 8) and num.network.threads (default 3/listener) and how to size from the idle-ratio metrics, num.replica.fetchers (replication parallelism), socket buffers, log.segment.bytes, queued.max.requests. The PAGE CACHE is the performance engine: keep heaps small, let the OS cache the tail, beware cache eviction by cold reads (mechanism, Part I 03/09). The end-to-end LATENCY BUDGET broken into request-queue / local-processing / remote (replication) / response-queue / throttle time (the RequestMetrics breakdown — Part I 06/07). OS knobs: vm.swappiness=1, vm.max_map_count, file descriptors, vm.dirty ratios.',
    files: 'clients/src/main/java/org/apache/kafka/clients/producer/ProducerConfig.java; clients/src/main/java/org/apache/kafka/clients/consumer/ConsumerConfig.java; server config ReplicationConfigs/ServerConfigs (threads); the empirical reference tuning. Cross-ref Part I 16,17,03,09,06,07.' },

  { n: 'op06', disp: 'II · 06', slug: 'op06-durability', title: 'Durability, Availability & Consistency',
    focus: 'The durability/availability/consistency dials and their mechanisms. acks (0/1/all) = the producer\'s durability request; min.insync.replicas = the broker\'s enforcement (with acks=all, ISR < minISR → NotEnoughReplicasException BEFORE the write — Part I 08); replication.factor = redundancy. Why RF=3 / min.insync.replicas=2 / acks=all is the durable-and-available standard: tolerates the loss of one in-sync broker with zero acknowledged-data loss while still accepting writes; show the arithmetic and the failure scenarios. unclean.leader.election.enable=false (default) — the availability-vs-durability dial; enabling it trades possible data loss for availability when all ISR replicas are down. THE FLUSH PHILOSOPHY: Kafka does not fsync each message; durability comes from REPLICATION across independent brokers plus the OS page cache, with log.flush.* off by default — so a simultaneous power loss of all ISR replicas can lose not-yet-flushed data, which is why ISR replicas must be failure-independent (rack/AZ spread). Rack awareness (broker.rack → StripedReplicaPlacer spreads replicas). ELR / KIP-966 (eligible leader replicas) for safer recovery. The operational CAP positioning. Durability math: probability of loss vs RF and failure independence.',
    files: 'core/src/main/scala/kafka/cluster/Partition.scala (min.isr enforcement, HW); server/src/main/java/org/apache/kafka/server/config/ReplicationConfigs.java (defaults); storage LogConfig (flush); metadata/src/main/java/org/apache/kafka/metadata/placement/StripedReplicaPlacer.java (rack); the flush path in UnifiedLog/LogManager. Cross-ref Part I 08,03,12.' },

  { n: 'op07', disp: 'II · 07', slug: 'op07-failure-modes', title: 'Failure Modes & The Runbook',
    focus: 'The architecture-derived failure catalogue as a runbook. For EACH: CAUSE → SYMPTOM (the specific metric) → MECHANISM (the source path) → MITIGATION. Cover: broker loss (URP, leader elections); log-directory / disk failure (LogDirFailureChannel marks the dir failed, its partitions go offline — Part I 03/08); controller quorum loss (no metadata progress, can\'t elect leaders or create topics — needs a majority of controllers, Part I 10/11); network partition (ISR shrink, possible unclean election); slow follower (ISR shrink via replica.lag.time.max.ms, URP — Part I 08); under-min-ISR (acks=all produce refused with NotEnoughReplicas); offline partitions (unavailable); unclean leader election (data loss); hot/skewed partition (one leader saturated — keying / partition count); GC pauses (session timeouts, ISR drop, soft outages); page-cache pressure / cold reads (disk read storms, latency); full disk (log dir offline / broker stop); request-handler & purgatory saturation (high request-queue time, throttling — Part I 06/07); fetch-session cache eviction (full fetches, extra load — Part I 09); consumer rebalance storm (max.poll.interval.ms / session.timeout.ms; cooperative & KIP-848 mitigations — Part I 13); producer fencing (ProducerFenced on idempotent/txn epoch bump — Part I 14); hanging transaction (LSO stuck → read_committed consumers blocked — Part I 14); metadata lag (stale routing). Tie each symptom to the metric in op08.',
    files: 'storage/src/main/java/org/apache/kafka/storage/internals/log/LogDirFailureChannel.java; core/src/main/scala/kafka/server/ReplicaManager.scala (ISR, offline); core/src/main/scala/kafka/cluster/Partition.scala; the empirical reference failure-ops. Cross-ref Part I 08,09,10,11,13,14,06.' },

  { n: 'op08', disp: 'II · 08', slug: 'op08-metrics-signals', title: 'Metrics, Signals & Observability',
    focus: 'What to watch and where it is emitted in source. The metrics plumbing (Yammer/KafkaMetricsGroup → JMX; client metrics via Kafka Metrics). THE GOLDEN SIGNALS, prioritized, each with where it comes from and its alert meaning: UnderReplicatedPartitions (>0 = degraded), OfflinePartitionsCount (>0 = outage), ActiveControllerCount (must sum to exactly 1 across the cluster), UnderMinIsrPartitionCount (acks=all at risk), the REQUEST LATENCY BREAKDOWN per API (RequestMetrics: request-queue / local / remote / throttle / response-queue time — the single most useful diagnostic), RequestHandlerAvgIdlePercent (→0 = io threads saturated), NetworkProcessorAvgIdlePercent, LogFlushRateAndTimeMs, LeaderElectionRateAndTimeMs, IsrShrinks/IsrExpandsPerSec, Produce/Fetch purgatory size, BytesIn/BytesOut/MessagesInPerSec (BrokerTopicMetrics), the controller event-queue time/size, and consumer lag (records-lag-max client-side + admin/group offsets). Give a thresholds table and a 12-panel dashboard blueprint. Client telemetry push (KIP-714, ClientMetricsManager — Part I 19).',
    files: 'storage/src/main/java/org/apache/kafka/storage/log/metrics/BrokerTopicMetrics.java; find RequestMetrics / RequestChannel metrics (grep "RequestQueueTimeMs","LocalTimeMs","RemoteTimeMs","ThrottleTimeMs" in core/server); controller metrics (metadata ControllerMetrics / QuorumControllerMetrics); purgatory metrics (server-common DelayedOperationPurgatory); server/src/main/java/org/apache/kafka/server/ClientMetricsManager.java. Cross-ref Part I 06,07,08,11,19.' },

  { n: 'op09', disp: 'II · 09', slug: 'op09-topologies', title: 'Topologies & Deployment Patterns',
    focus: 'How to lay out a cluster. KRaft node layout: dedicated controllers (an odd quorum of 3 or 5 — tolerate floor((n-1)/2) failures; isolation from data load) vs combined broker+controller (small clusters only); controller.quorum.voters / dynamic voters (KIP-853). RACK / MULTI-AZ awareness: broker.rack → StripedReplicaPlacer spreads each partition\'s replicas across racks so an AZ loss does not take partitions offline (interplay with min.insync.replicas; and the cross-AZ traffic cost it creates — see op10). Single vs multi-cluster (blast radius, tenant isolation). Multi-region: MirrorMaker 2 (the connect/mirror module — async replication, offset translation, active/active vs active/passive — Part I 21); stretch clusters (synchronous across nearby AZs, latency cost). Tiered-storage topology (an object store behind the cluster — Part I 05). Tenancy: shared cluster with quotas+ACLs vs dedicated clusters. A recommended topology per scale tier (dev / medium / large / global), as a decision guide.',
    files: 'core/src/main/scala/kafka/server/KafkaRaftServer.scala; raft/src/main/java/org/apache/kafka/raft/QuorumConfig.java; metadata/src/main/java/org/apache/kafka/metadata/placement/StripedReplicaPlacer.java; connect/mirror/* (MirrorMaker2); storage remote log (Part I 05). Cross-ref Part I 05,10,11,21.' },

  { n: 'op10', disp: 'II · 10', slug: 'op10-cost', title: 'Cost Engineering',
    focus: 'The cost drivers, rooted in architecture, and the levers. DRIVERS: STORAGE = ingress × retention × RF on local disk (RF multiplies it); NETWORK = replication amplification (ingress × (RF-1) inter-broker) PLUS cross-AZ data transfer — in the cloud, replication and consumer fetch that cross AZ boundaries are billed, and this is frequently the largest line item; COMPUTE = brokers (CPU for compression/TLS/handling). THE LEVERS, each with mechanism and the source/feature: compression.type (cuts storage AND network at CPU cost; zstd/lz4 — research numbers); fetch-from-follower / rack-aware replica selection (KIP-392) so consumers read a same-AZ replica → eliminates cross-AZ READ cost (Part I 09); tiered storage (KIP-405) to push cold data to cheap object storage and shrink the RF-multiplied local disk (Part I 05); replication.factor and retention as cost dials (RF=3 standard vs RF=2 risk; shorter retention or tiered); and the emerging diskless / object-store-native designs (WarpStream, KIP-1150) that restructure the cost model. Real figures from the research reference (e.g. Confluent Cloud ~$13/partition/year; cross-AZ $/GB). A worked cost model and an effort/impact-ranked lever table.',
    files: 'the empirical reference cost section (primary); clients compression (Part I 01); fetch-from-follower replica selector (Part I 09); RemoteLogManager (Part I 05). Cross-ref Part I 01,05,09,08.' },

  { n: 'op11', disp: 'II · 11', slug: 'op11-scaling-scenarios', title: 'Scaling: 1M → 10M → 100M events/sec',
    focus: 'Three worked capacity tiers, each: stated assumptions (e.g. 1 KiB avg message), the throughput math, partition count = max(T/Tp, T/Tc), broker count, RF, the BOTTLENECK that emerges, and exactly what to monitor. 1M events/s (~1 GB/s @ 1 KiB): straightforward — ~100–200 partitions, a handful of brokers, page cache holds the tail; watch consumer lag and URP. 10M/s (~10 GB/s): partition count grows to the low thousands, replication network (×RF) becomes a major NIC consumer, the page-cache working set may exceed RAM (cold reads), num.replica.fetchers and segment/flush tuning matter, cross-AZ replication cost appears; watch network saturation, ISR shrink, page-cache hit. 100M/s (~100 GB/s): you hit STRUCTURAL limits — partition count vs the per-broker ceiling and metadata, raw network (needs many brokers / high-bandwidth NICs), cross-AZ cost dominates (fetch-from-follower becomes mandatory), tiered storage is effectively required, and you may need multi-cluster federation; watch controller event-queue, metadata lag, network, and cost. Anchor to real numbers (LinkedIn ~7 trillion msgs/day ≈ 80M/s across 100+ clusters — research). For each tier, state WHERE Kafka\'s limits bind and the architectural reason, and which limits are tunable vs structural.',
    files: 'the empirical reference (case studies + formulas, primary); Part I 08,09,11,05 for the mechanisms that bind. Cross-ref op02,op03,op04,op10.' },

  { n: 'op12', disp: 'II · 12', slug: 'op12-lifecycle', title: 'Lifecycle Operations',
    focus: 'Running a cluster over time. ROLLING UPGRADES: upgrade binaries one broker at a time (controlled shutdown moves leadership), then bump metadata.version via the feature mechanism (KIP-584, FeatureControlManager) to enable new record formats/behaviours — and the hard rule that metadata.version generally cannot be downgraded; the compatibility ordering. PARTITION REASSIGNMENT: kafka-reassign-partitions → AlterPartitionReassignments to the controller (ReplicationControlManager) moves/adds/removes replicas; THROTTLE the move with the replication quota (leader.replication.throttled.rate / follower; ReplicationQuotaManager) so it does not starve live traffic; monitor progress. ADDING/REMOVING BROKERS: registration & fencing (BrokerLifecycleManager — Part I 12); there is no automatic data rebalance onto new brokers (you reassign; tools like Cruise Control automate it); decommissioning safely. Preferred-leader election to rebalance leadership. CLUSTER FORMATION recap (kafka-storage format — Part I 11). DYNAMIC CONTROLLER membership (KIP-853 AddVoter/RemoveVoter — Part I 10). DISASTER RECOVERY: the __cluster_metadata snapshot, MirrorMaker2 for cross-cluster backup, unclean recovery, JBOD disk replacement.',
    files: 'core/src/main/scala/kafka/tools/StorageTool.scala; metadata controller ReplicationControlManager (reassignment), FeatureControlManager (metadata.version); core/server ReplicationQuotaManager (throttle); server BrokerLifecycleManager; raft AddVoterHandler (KIP-853). Cross-ref Part I 10,11,12.' },

  { n: 'op13', disp: 'II · 13', slug: 'op13-multitenancy', title: 'Multitenancy & Isolation: What is Shared, What is Not',
    focus: 'The honest, source-grounded answer to "how isolated are tenants on a shared Kafka cluster?" — mostly NOT. THE CENTRAL TRUTH: a Kafka broker is a SHARED-EVERYTHING process; the great majority of resources are POOLED across all clients, topics and tenants, so different producers/consumers of different topics CAN and routinely DO elevate each other\'s latency and error rates (the noisy-neighbour problem). Lay this out as two halves with the interference path and the metric that reveals each.\n' +
      'WHAT IS NOT ISOLATED (shared → cross-tenant interference): (1) the REQUEST-HANDLER thread pool — num.io.threads / KafkaRequestHandler is shared by ALL requests of all tenants; one tenant\'s heavy or slow requests (huge fetches, many partitions per request, slow disk reads) occupy handler threads, so the bounded request queue backs up and RequestQueueTimeMs / total latency rises for EVERYONE (mechanism Part I 06,07). (2) the network Processor pool and the bounded RequestChannel queue (queued.max.requests, default 500) — a flood from one tenant fills it → backpressure / muting for all connections on the broker. (3) the OS PAGE CACHE — shared; a tenant reading large COLD/historical data evicts the hot tail of other tenants\' partitions, knocking their consumers off the zero-copy path onto disk reads → their fetch latency spikes (the classic noisy neighbour; mechanism Part I 03,09). (4) DISK I/O and the NIC — one set per broker, shared by replication + every producer/consumer + the log cleaner + cold reads. (5) the REPLICA-FETCHER threads (num.replica.fetchers) — shared; a hot partition\'s replication delays others\' (Part I 09). (6) the CONTROLLER — a single cluster-wide writer; one tenant churning topic create/delete or dynamic-config changes loads the controller event queue → metadata operations slow for ALL (Part I 11). (7) the COORDINATORS — __consumer_offsets and __transaction_state are shared internal logs; a group hashes to a coordinator partition, so one tenant\'s rebalance storm or high offset-commit rate loads the shard that other tenants\' groups share (Part I 13,14). (8) the JVM HEAP and GC — shared; one tenant\'s allocation pressure → a GC pause → ISR drops and session timeouts across the whole broker. (9) the log-cleaner threads; (10) connections, file descriptors and memory-maps (the per-broker partition ceiling, op02).\n' +
      'WHAT IS ISOLATED (or partially): (a) DATA / STORAGE — each partition is a physically separate log (its own files); reads/writes do not share data structures, and ACCESS is gated by ACLs (Part I 18) — a tenant cannot read another\'s data. (b) QUOTAS — the PRIMARY multitenancy mechanism (ClientQuotaManager, Part I 19): produce-rate, fetch-rate, request-rate (request.percentage = a share of io+network thread time), and controller-mutation quotas, per (user / client-id / IP), THROTTLE a tenant over its share by delaying the response and muting the channel — BUT the crucial caveats: quotas are RATE limits applied AFTER the work is done on the shared threads (they delay the reply, the handler time was already spent), they do NOT isolate page-cache eviction or a tenant\'s head-of-line position in the shared queue, and a tenant comfortably UNDER its quota can still cause cache/disk interference. (c) LISTENERS — separating inter-broker, client and the controller control-plane onto distinct listeners/ports isolates some request classes (control-plane vs data-plane Processors). (d) CONNECTION quotas (max.connections, max.connections.per.ip). (e) DEDICATED CLUSTERS — the ONLY full resource isolation (separate brokers + controllers), at cost. Present the ISOLATION SPECTRUM: shared cluster + quotas + ACLs (cheap, soft, noisy-neighbour risk) → resource groups / careful placement → dedicated clusters per tenant (full, expensive). Deliver a big table (resource | shared or isolated? | isolation mechanism if any | interference symptom & metric | mitigation) and 3-4 concrete noisy-neighbour scenarios (cold-read cache eviction; produce-flood queue saturation; topic-churn controller load; rebalance-storm coordinator load), each tied to the metric in op08 that reveals it. Close with isolation-mode recommendations per tenancy model.',
    files: 'core/src/main/scala/kafka/server/KafkaRequestHandler.scala (shared handler pool); core/src/main/scala/kafka/network/{SocketServer.scala (Processor, ConnectionQuotas), RequestChannel.scala (queued.max.requests)}; server/src/main/java/org/apache/kafka/server/quota/{ClientQuotaManager.java, ControllerMutationQuotaManager.java, ClientRequestQuotaManager.java} (request.percentage, throttling); server/src/main/java/org/apache/kafka/network/SocketServerConfigs.java (max.connections); FileRecords (page cache/zero-copy, Part I 03/09); metadata QuorumController (event queue); group-coordinator (coordinator partitions); clients common/acl (ACLs). Cross-ref Part I 06,07,03,09,11,13,18,19; Part II op02,op08,op09.' },

  { n: 'op14', disp: 'II · 14', slug: 'op14-proactive-monitoring', title: 'Proactive Monitoring: Leading Indicators & Capacity Runway',
    focus: 'How to see trouble coming BEFORE it lands — the leading indicators that buy lead time, as opposed to the lagging golden signals in op08-metrics-signals (UnderReplicatedPartitions, OfflinePartitionsCount, UncleanLeaderElectionsPerSec) which only fire once damage is already done. THE CORE DISTINCTION up front: a LAGGING indicator tells you it already broke; a LEADING indicator is a TREND (slope / rate of change), a SATURATION early-warning band, or a HEADROOM/RUNWAY metric that predicts the break with lead time. Three monitoring disciplines to teach explicitly: (A) alert on the SLOPE, not just the threshold (a metric trending toward a cliff) and how to do rate-of-change / burn-rate alerts; (B) track HEADROOM / RUNWAY (how far, or how many days, until a limit); (C) watch SATURATION bands (e.g. thread-idle % entering a danger zone) long before queuing and errors appear.\n' +
      'THE LEADING-INDICATOR CATALOGUE — for EACH give: what it predicts, the early-warning band or trend to alert on, the typical lead time it buys, and its source. (1) CONSUMER LAG TREND — the SLOPE of records-lag plus time-lag (not the absolute value): a sustained positive slope means consumers are below required throughput and predicts an SLA breach / retention overrun well before lag is "large" (cross-ref op03, Part I 17). (2) RequestHandlerAvgIdlePercent DECLINING into a band (~30% = warning, →0 = saturated) predicts I/O-thread saturation and rising RequestQueueTimeMs before requests time out (Part I 06). (3) NetworkProcessorAvgIdlePercent declining — same for network threads. (4) PAGE-CACHE pressure: disk-read rate climbing while consumers are caught up predicts the working set outgrowing RAM and consumers about to fall off the zero-copy path (op04; Part I 03/09). (5) IsrShrinksPerSec ticking up (shrinks that re-expand) — an early sign brokers are struggling (GC/disk/network) before sustained URP or offline partitions (Part I 08). (6) DISK FREE RUNWAY (days-to-full = free_bytes / (ingress_per_day × RF)) predicts a full-disk outage days ahead (op04). (7) PARTITION-COUNT HEADROOM (% of the per-broker ceiling from op02) predicts hitting the limit and slow failover. (8) p99 PRODUCE/FETCH LATENCY DRIFT — a slow upward trend predicts an SLA breach before it crosses the alert line. (9) GC pause frequency/duration trending up predicts soft failures (ISR drops, session timeouts) cascading. (10) Quota THROTTLE-TIME becoming non-zero predicts client-visible slowdown/errors (op19, op13). (11) CONTROLLER event-queue time/backlog rising predicts metadata-operation slowness (Part I 11). (12) REBALANCE RATE increasing predicts group instability / processing stalls (Part I 13). (13) CONNECTION count / creation-rate approaching caps. (14) LOG-FLUSH time creeping up = disk slowing. (15) LEADER-ELECTION time growing with partition count predicts longer outages on the NEXT failure.\n' +
      'CLIENT-TEAM LEADING INDICATORS (the part most guides miss — what producer/consumer APP teams can watch from their OWN client JMX metrics, with NO broker access, to be proactive from the application side). CONSUMER: records-lag-max and its TREND, the fetch-latency-avg drift, rebalance-rate / failed-rebalance-rate, commit-latency, the poll interval vs max.poll.interval.ms headroom (are you processing fast enough to avoid being kicked?), bytes-consumed-rate vs the production rate. PRODUCER: record-queue-time-avg/-max RISING (the RecordAccumulator backing up — the broker can\'t keep up or batching is mis-tuned — predicts buffer exhaustion, then max.block.ms timeouts and send errors), buffer-available-bytes FALLING toward zero (blocking imminent), request-latency-avg drift, record-retry-rate and record-error-rate creeping up, produce-throttle-time-avg becoming non-zero (you are approaching a quota). Explain that these client-side metrics (standard Kafka client JMX, and pushed centrally via KIP-714 client telemetry, Part I 19) let an app team see trouble building BEFORE the platform team\'s lagging alerts fire — and give a concrete "producer/consumer proactive checklist". \n' +
      'Then a RUNWAY / HEADROOM DASHBOARD spec (the "how much runway is left" panel set: throughput headroom via idle %, disk days-to-full, partition headroom, FD/connection headroom, consumer-lag-time runway, cache headroom) and the SLO ERROR-BUDGET / BURN-RATE framing (alert when, at the current rate, you will exhaust the budget — the proactive SRE pattern). Deliver a big table: leading indicator | what it predicts | early-warning band/trend | typical lead time | source/metric. Make the lagging-vs-leading contrast the spine of the chapter.',
    files: 'broker idle-ratio metrics in core/src/main/scala/kafka/server/KafkaRequestHandler.scala and kafka/network/SocketServer.scala; client metrics — clients/src/main/java/org/apache/kafka/clients/producer/internals/{Sender.java, RecordAccumulator.java, SenderMetricsRegistry.java, BufferPool.java} (record-queue-time, buffer-available-bytes, record-retry/error-rate), clients/src/main/java/org/apache/kafka/clients/consumer/internals/* (records-lag, fetch-latency, rebalance metrics); server/src/main/java/org/apache/kafka/server/ClientMetricsManager.java (KIP-714); storage BrokerTopicMetrics. Cross-ref Part I 06,08,11,13,16,17,19; Part II op04,op07,op08,op11,op13.' }
];

const AUTHOR_SCHEMA = {
  type: 'object',
  properties: {
    fragmentPath: { type: 'string' }, title: { type: 'string' },
    abstract: { type: 'string' },
    sections: { type: 'array', items: { type: 'string' } },
    keyFacts: { type: 'array', items: { type: 'string' }, description: '6-12 citable operational facts (with source) for cross-part synthesis' },
    sourceFilesRead: { type: 'array', items: { type: 'string' } },
    diagrams: { type: 'number' }, legends: { type: 'number' },
    approxWords: { type: 'number' }
  },
  required: ['fragmentPath', 'title', 'abstract', 'sections', 'keyFacts']
};

const VERIFY_SCHEMA = {
  type: 'object',
  properties: {
    slug: { type: 'string' }, accuracyScore: { type: 'number' },
    htmlOk: { type: 'boolean' }, mermaidRemaining: { type: 'number' }, legendsOk: { type: 'boolean' },
    errors: { type: 'array', items: { type: 'object', properties: {
      severity: { type: 'string', enum: ['critical', 'major', 'minor'] },
      claim: { type: 'string' }, correction: { type: 'string' } }, required: ['severity', 'claim', 'correction'] } },
    verdict: { type: 'string', enum: ['publishable', 'minor-fixes', 'needs-rework'] }
  },
  required: ['slug', 'accuracyScore', 'errors', 'verdict']
};

function authorPrompt(s) {
  const out = OUT + '/_fragments/' + s.slug + '.html';
  return HOUSE + '\n\n' + COMPONENTS + '\n\n' + XLINKS + '\n\n=== YOUR CHAPTER ===\n' +
    'h1 text: "' + s.disp + ' · ' + s.title + '".\nWrite the complete HTML fragment to: ' + out + '\n\n' +
    'SCOPE & THE QUESTIONS THIS CHAPTER MUST ANSWER:\n' + s.focus + '\n\n' +
    'SOURCE STARTING POINTS (read them; grep to find exact constants/defaults; the code is truth):\n' + s.files + '\n\n' +
    'Read the relevant source AND the empirical reference, then write the dense, practical, source-grounded chapter. Every default/limit/metric cited to source (path:line); every benchmark/heuristic cited to the reference\'s named source and marked as empirical. Use bespoke diagrams with legends. After writing, return the structured summary.';
}

function verifyPrompt(s) {
  const out = OUT + '/_fragments/' + s.slug + '.html';
  return 'Adversarially fact-check this Apache Kafka 4.4 OPERATIONS chapter against the source. Read the fragment at ' + out + ' and independently verify against source under ' + ROOT + ' (start: ' + s.files + '). Check: every config KEY exists and its DEFAULT is correct; every hardcoded limit/constant is real and correctly classified (hard/soft/emergent); metric names exist; mechanism claims match the code; no invented configs/metrics; nothing treats ZooKeeper as current (KRaft-only); empirical/benchmark numbers are presented AS empirical (not as hard guarantees) and attributed. Also check HTML: no Mermaid/ASCII diagrams remain, every <figure class="diagram"> has a <div class="legend">, well-formed. Report concrete errors with the source-grounded correction (path:line). Score 0-100; verdict publishable / minor-fixes / needs-rework. Set mermaidRemaining and legendsOk.';
}

async function withRetry(items, fn, rounds) {
  rounds = rounds || 3;
  let out = await parallel(items.map((it) => () => fn(it)));
  for (let r = 1; r <= rounds; r++) {
    const todo = items.filter((it, i) => out[i] == null);
    if (!todo.length) break;
    log('Retry round ' + r + ' for ' + todo.length + ' chapter(s).');
    const redo = await parallel(todo.map((it) => () => fn(it)));
    todo.forEach((it, k) => { out[items.indexOf(it)] = redo[k]; });
  }
  return out;
}

// args may be an array of slugs to (re)author; empty => all.
// NOTE: the workflow `args` global was not reaching the script in this environment,
// so this run is hardcoded to the single new chapter to avoid re-authoring everything.
const filter = Array.isArray(args) && args.length ? args.map(String) : ['op14-proactive-monitoring'];
const wanted = SUBSYSTEMS.filter((s) => filter.includes(s.slug));

phase('Author');
const authored = await withRetry(wanted, (s) => agent(authorPrompt(s), {
  label: 'author:' + s.slug, phase: 'Author', schema: AUTHOR_SCHEMA, agentType: 'general-purpose'
}));

phase('Verify');
const results = await withRetry(wanted, (s, i) => {
  if (!authored[i]) return Promise.resolve({ slug: s.slug, failed: true });
  return agent(verifyPrompt(s), { label: 'verify:' + s.slug, phase: 'Verify', schema: VERIFY_SCHEMA, agentType: 'general-purpose' })
    .then((v) => ({ slug: s.slug, authored: authored[i], verify: v }));
});

const ok = authored.filter(Boolean).length;
log('Part II authored: ' + ok + '/' + wanted.length + ' chapters.');
return results;
